{
  "name": "@topox/interop",
  "version": "0.1.0",
  "description": "TopoX interop: Mermaid / DOT / draw.io / YAML importers and exporters that translate foreign formats to TopoDoc.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@topox/core": "*",
    "@xmldom/xmldom": "^0.9.10",
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^3.2.6"
  }
}
